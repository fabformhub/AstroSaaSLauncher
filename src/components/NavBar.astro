<nav class="w-full py-6 px-8 flex items-center justify-between border-b-4 border-[#0F172A] bg-white relative">

  <!-- Scribble Accent (top-left) -->
  <svg class="absolute -top-4 left-4 w-32 h-12 opacity-50 pointer-events-none"
       viewBox="0 0 200 60" fill="none" xmlns="http://www.w3.org/2000/svg">
    <path d="M5 30 C40 5, 80 55, 120 20 S180 40, 195 10"
          stroke="#A259FF" stroke-width="4" stroke-linecap="round"
          class="animate-scribble-nav" />
  </svg>

  <!-- Logo -->
  <div class="flex items-center gap-3">
    <div class="w-8 h-8 rounded-sm bg-[#0F172A]"></div>
    <span class="text-2xl font-black tracking-tight text-[#0F172A]">
      AstroSaaSLauncher
    </span>
  </div>

  <!-- Nav Links -->
  <div class="hidden md:flex items-center gap-10 text-lg font-semibold">
    <a href="/features" class="nav-link">Features</a>
    <a href="/pricing" class="nav-link">Pricing</a>
    <a href="/docs" class="nav-link">Docs</a>
    <a href="/contact" class="nav-link">Contact</a>
  </div>

  <!-- CTA -->
  <a href="/signup"
     class="px-6 py-3 text-lg font-bold border-2 border-[#0F172A] rounded-lg hover:bg-[#0F172A] hover:text-white transition-all duration-300">
    Start Now
  </a>

</nav>

<style>
  /* Scribble Animation */
  @keyframes scribble-nav {
    0% { stroke-dashoffset: 0; transform: translateY(0px); }
    50% { stroke-dashoffset: -60; transform: translateY(-4px); }
    100% { stroke-dashoffset: 0; transform: translateY(0px); }
  }

  .animate-scribble-nav {
    animation: scribble-nav 4s ease-in-out infinite;
  }

  /* Brutalist Hover Underline */
  .nav-link {
    position: relative;
    color: #0F172A;
    padding-bottom: 4px;
  }

  .nav-link::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: -2px;
    width: 0%;
    height: 3px;
    background: linear-gradient(90deg, #A259FF, #3C82F6);
    transition: width 0.3s ease;
  }

  .nav-link:hover::after {
    width: 100%;
  }
</style>

